{{#each components.schemas}}
{{#if description}}
/**
 * {{description}}
 */
{{/if}}
export class {{@key}} {
  {{#each properties}}
    {{#if description}}
    // {{description}}
    {{/if}}
    public {{@key}}{{#unless _required}}?{{/unless}}: {{typeConvert this}};
  {{/each}}
}
{{/each}}