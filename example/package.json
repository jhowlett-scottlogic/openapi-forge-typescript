{
  "name": "openapi-forge-typescript-example",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "generate-api": "openapi-forge forge ./petstore.json .. -o api",
    "start": "npm run generate-api && ts-node index.ts"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.1"
  }
}
