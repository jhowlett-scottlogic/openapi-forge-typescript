{
  "name": "openapi-forge-typescript",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "test:generate": "openapi-forge forge ./test/test-api.json . -o test/api --exclude=nodeFetch.ts",
    "test:execute": "jest",
    "test": "npm run test:generate && npm run test:execute"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^27.5.1",
    "jest": "^28.1.0",
    "ts-jest": "^28.0.3",
    "typescript": "^4.6.4"
  }
}
