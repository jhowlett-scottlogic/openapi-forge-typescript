{{#if models}}
  {{#if options.testRun }}
    const {
      {{#each models}}{{@key}}, {{/each}}
    } = require("./model");
  {{else}}
    import {
      {{#each models}}{{@key}}, {{/each}}
    } from "./model";
  {{/if}}
{{/if}}